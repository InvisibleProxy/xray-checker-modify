# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# Environment files
.env
.env.*
!.env.example
probe-agent.env

# Local reverse-proxy configuration
Caddyfile

# Application runtime state
data/
*.log
*.tmp
*.pid

# Local build, test, and profiling output
dist/
build/
tmp/
.cache/
coverage/
coverage.*
*.coverprofile

# Local editor and OS metadata
.idea/
.vscode/
*.swp
*.swo
Thumbs.db
Desktop.ini

# GitHub automation inherited from upstream stays disabled in this fork. The
# directory itself is not ignored, so the probe-agent publish workflow this fork
# owns can be tracked; anything else pulled in from upstream is still skipped.
.github/workflows/*
!.github/workflows/probe-agent-image.yml
.github/dependabot.yml

xray-checker
xray_config.json
docker-compose.yml
docker-compose.yaml
docker-compose.override.yml
docker-compose.override.yaml
compose.yml
compose.yaml
compose.override.yml
compose.override.yaml
.DS_Store
test-files/
configs/
geo/
.claude/
TODO.md
